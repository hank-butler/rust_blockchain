[package]
name = "rust_blockchain"
version = "0.1.0"
edition = "2021"

[dependencies]
sha2 = "0.10.8"
serde = {version = "1.0.193", features = ["derive"]}
serde_json = "1.0.108"
hex = "0.4.3"
chrono = "0.4.38"
rocket = "0.5.0"
rusqlite = "0.30.0"
dotenv = "0.15.0"
rand = "0.8.5"
log = "0.4.22"

# Old dependencies
# # Serialization and deserialization
# serde = { version = "1.0.193", features = ["derive"] }
# serde_json = "1.0.108"

# # Cryptographic utilities
# sha2 = "0.10.8"   # SHA-2 hashing
# # ed25519-dalek = "2.1.1"  # Digital signatures
# rand = "0.8.5"   # Random number generation
# hex = "0.4.3"    # Hexadecimal encoding/decoding
# num-bigint = "0.4.3"  # Handling large numbers

# # Blockchain-specific utilities
# # libp2p = { version = "0.54.1", features = ["tcp-tokio", "dns-tokio"] }  # P2P networking

# # Time and scheduling
# chrono = "0.4.38"  # Date and time handling
# periodic = "0.1.1"  # Periodic tasks

# # Async and runtime utilities
# # tokio = { version = "1.40.0", features = ["io-util", "io-std", "macros", "rt", "rt-multi-thread", "sync", "time"] }
# once_cell = "1.19.0"  # For efficient single initialization
# log = "0.4.22"  # Logging

# # Miscellaneous
# # uuid = { version = "1.10.0", features = ["serde", "v4"] }
# rand = "0.8.5"
